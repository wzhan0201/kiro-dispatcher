# Local data

This directory holds operator-specific, gitignored state such as:
- `captain.md` — durable preferences loaded into the dispatcher prompt;
- `backlog.md` — optional local task queue;
- `learnings.md` — evidence-backed operational facts.

`bin/dispatch-init.sh` creates the standard local files.
